# Use the official NGINX image from the Docker Hub as a base image
FROM nginx:alpine

# Copy your custom NGINX configuration file to the container
# Uncomment the following line if you have a custom nginx.conf
# COPY nginx.conf /etc/nginx/nginx.conf

# Copy static website files to the appropriate directory in the container
# Uncomment the following line if you have website files to include
# COPY /path/to/your/website /usr/share/nginx/html

# Expose port 80 for the web server
EXPOSE 80

# Start NGINX when the container launches
CMD ["nginx", "-g", "daemon off;"]
